public class CharacterSetupMemory
{
    /// <summary>
    /// An array of option indexes. The indexes are ordered the same way the 'Options' enum is ordered.
    /// TODO: Find a way to make this more secure?
    /// </summary>
    public int[] indexes = { 0, 0, 0 };
}